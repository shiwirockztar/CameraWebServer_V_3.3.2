{
  "name": "ESP32 Camera - PlatformIO (Codespaces)",
  // Imagen base para el Codespace: uso una imagen Node.js común que es útil para
  // desarrollo general (puedes cambiar a una imagen Python si lo prefieres).
  "image": "mcr.microsoft.com/devcontainers/javascript-node:18-bullseye",

  /*
    Descripción y notas:
    - La propiedad "customizations.vscode" contiene las extensiones y settings que
      Codespaces / Dev Containers aplica automáticamente.
    - He eliminado las propiedades no estándar "description" y "devcontainerNotes"
      para evitar advertencias de esquema. Consulta `.devcontainer/README.md` para
      notas extensas.
  */

  "customizations": {
    "vscode": {
      "extensions": [
        // Lista de extensiones detectadas/indicadas por el usuario. Se instalarán
        // automáticamente en el Codespace al abrir el contenedor.
        "angular.ng-template",
        "benjaminbenais.copilot-theme",
        "docker.docker",
        "dracula-theme.theme-dracula",
        "endormi.2077-theme",
        "esbenp.prettier-vscode",
        "ghgofort.neon-vommit",
        "github.copilot",
        "github.copilot-chat",
        "github.copilot-labs",
        "github.remotehub",
        "kisstkondoros.vscode-gutter-preview",
        "mechatroner.rainbow-csv",
        "mhutchie.git-graph",
        "ms-azuretools.vscode-containers",
        "ms-azuretools.vscode-docker",
        "ms-dotnettools.csdevkit",
        "ms-dotnettools.csharp",
        "ms-dotnettools.vscode-dotnet-runtime",
        "ms-dotnettools.vscodeintellicode-csharp",
        "ms-vscode-remote.remote-containers",
        "ms-vscode.azure-repos",
        "ms-vscode.cmake-tools",
        "ms-vscode.cpptools",
        "ms-vscode.cpptools-extension-pack",
        "ms-vscode.cpptools-themes",
        "ms-vscode.remote-repositories",
        "ms-vsliveshare.vsliveshare",
        "pkief.material-icon-theme",
        "platformio.platformio-ide",
        "rapidapi.vscode-rapidapi-client",
        "rapidapi.vscode-services",
        "tabnine.tabnine-vscode",
        "usernamehw.errorlens"
      ],

      // Ajustes del editor a aplicar dentro del Codespace. Estos valores son
      // asumidos como ejemplos basados en tu petición; cámbialos si quieres que
      // reflejen exactamente tu `settings.json` local.
      "settings": {
        // Fuente y ligaduras
        "editor.fontFamily": "'Fira Code', 'Cascadia Code', Consolas, 'Courier New', monospace",
        "editor.fontLigatures": true,
        // Tamaño y zoom
        "editor.fontSize": 14,
        "window.zoomLevel": 0,
        // Tema de color: cambia a "Neon Vommit" si ese es el nombre exacto del theme
        "workbench.colorTheme": "Neon Vommit",
        // Barra de menú
        "window.menuBarVisibility": "compact",
        // Minimap y otras opciones visuales
        "editor.minimap.enabled": false,
        "editor.tabSize": 2,
        "editor.formatOnSave": true,
        "files.autoSave": "onFocusChange",
        // C/C++ y Python helpers
        "C_Cpp.clang_format_fallbackStyle": "Google",
        "python.linting.enabled": true,
        "python.linting.pylintEnabled": true
      }
    }
  },

  "postCreateCommand": "bash -lc 'set -e && apt-get update && apt-get install -y python3-pip python3-venv build-essential git curl && python3 -m pip install -U pip && pip install platformio'",

  "forwardPorts": [80, 8080, 8000],


  "remoteUser": "vscode"
}
