// camera_index.h (copied from original)
// This file contains gzipped HTML pages for the web UI. Keep it in src/ so it is compiled into the firmware.

// NOTE: The original file is large and already exists in the project root; this copy keeps the same symbols.

// For brevity in this automated copy, include the original file content from the workspace root if needed.

#include "../camera_index.h"
